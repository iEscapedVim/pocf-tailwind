---
const { filter } = Astro.props;
const { title, filtername } = filter.data;
---
<button data-target={filtername} class="outline-0 ring-0 focus:ring-0 focus:outline-0 capitalize border border-primary rounded-full py-2 px-5 hover:bg-primary hover:text-white" onclick="setActive(this)">{title}</button>